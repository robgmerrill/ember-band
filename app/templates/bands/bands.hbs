<div class="col-md-4">
  <div class="list-group">
    <div class="list-group-item">
      {{input type="text" class="new-band" placeholder="New band" value=name}}
      <button class="btn btn-primary btn-sm new-band-button" {{action
"createBand"}}>Add</button>
    </div>
    {{#each model as |band|}}
      (...)
    {{/each}}
  </div>
</div>
